spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/soccer_db
    username: user
    password: password123!

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
